import styled from "styled-components";

const MainPageWrapper = styled.div`

    & .infoTitle {
        text-align: center;
        padding-top: 40px;
    }

    & .infoDivs {
        padding-top: 7px;
        padding-bottom: 10px;
        margin-left: 40px;
        width: 550px;
      }

    img {
        float: left;
        padding: 0;
        margin: 0;
    }
`;

export default MainPageWrapper